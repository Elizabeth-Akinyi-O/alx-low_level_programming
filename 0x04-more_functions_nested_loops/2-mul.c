#include "main.h"
/**
 *mul - multiplies two integers and returns the result
 *@a: first number to be multiplied
 *@z: second number to be multiplied
 *Return: product
 */
int mul(int a, int z)
{
	int product = (a * z);

	return (product);
}
