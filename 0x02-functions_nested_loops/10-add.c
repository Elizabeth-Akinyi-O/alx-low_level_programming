#include "main.h"
/**
 *add - adds two integers and returns the result
 *@a: first number to be added
 *@z: second number to be added
 *Return: sum
 */
int add(int a, int z)
{
	int sum = (a + z);

	return (sum);
}
