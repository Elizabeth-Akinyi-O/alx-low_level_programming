#ifndef ABS_HEADER
#define ABS_HEADER
/**
 *ABS - computes the absolute value of a number x
 *@x: input value
 *Description: if x is -ve, then multiply x with -1
 *to make it positive, else print x
 */
#define ABS(x) ((x) < 0 ? (-1) * (x) : (x))

#endif /* 3-function_like_macro.h file ends here */
